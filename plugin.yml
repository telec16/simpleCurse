name: simpleCurse
prefix: simpleCurse
main: fr.telec.simpleCurse.SimpleCurse
version: 1.1
api-version: 1.15
load: POSTWORLD
authors:
- telec16
commands:
  update:
    description: Update the curses list and configuration
    usage: /<command>
    permission: <plugin name>.<command>
    permission-message: You don't have <permission>
  curses:
    aliases:
    - swears
    description: Shows the curses list
    usage: /<command>
    permission: <plugin name>.<command>
    permission-message: You don't have <permission>